# 统一认证中心配置文件

server:
  addr: ":8080"
  mode: "debug"  # debug, release
  read_timeout: "10s"
  write_timeout: "10s"

database:
  driver: "postgres"  # postgres 或 mysql
  
  postgres:
    host: "1.95.88.239"
    port: 5432
    user: "uac"
    password: "j4x3mdzttNnanCsB"
    dbname: "uac"
    sslmode: "disable"
    
  mysql:
    host: "1.95.88.239"
    port: 3306
    user: "uac"
    password: "j4x3mdzttNnanCsB"
    dbname: "uac"
    charset: "utf8mb4"
    parse_time: true
    loc: "Local"

redis:
  addr: "1.95.88.239:6379"
  password: "123456"
  db: 0

jwt:
  private_key_path: "./configs/keys/private.pem"
  public_key_path: "./configs/keys/public.pem"
  issuer: "unified-auth-center"
  access_expiry: "2h"
  refresh_expiry: "168h"  # 7 天

# 静态文件配置（前端嵌入）
static:
  enabled: true           # 是否启用静态文件服务
  mode: "embed"           # embed（嵌入到二进制）或 disk（从磁盘读取）
  path: "./web/dist"      # disk 模式下的文件路径
